{
  "name": "openmouse-desktop",
  "private": true,
  "version": "0.0.24",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:bridge": "tsc && vite build --mode bridge",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:bridge": "tauri build --config src-tauri/tauri.bridge.conf.json"
  },
  "dependencies": {
    "@openmouse/protocol": "https://github.com/OpenMouse-Project/mouse-protocol.git",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "lucide-preact": "^1.33.0",
    "preact": "^10.25.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.3",
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
