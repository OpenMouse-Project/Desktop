# Shell scripts must stay LF even on Windows checkouts — a CRLF line ending
# breaks the `#!/bin/sh` shebang and the deb/rpm maintainer scripts (bundled
# by tauri-bundler) fail to run on the Linux CI / install hosts.
*.sh text eol=lf
